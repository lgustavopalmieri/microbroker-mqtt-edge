services:
  microbroker-mqtt-edge:
    build: .
    container_name: microbroker-mqtt-edge
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "1883:1883"
      - "8080:8080"
    volumes:
      - broker-data:/data

volumes:
  broker-data:
